apiVersion: v1
kind: Secret
metadata:
  name: pgiceberg-secret
  namespace: walrus
  labels:
    app.kubernetes.io/part-of: walrus
    app.kubernetes.io/managed-by: kustomize
type: Opaque
stringData:
  # Replace with actual password or manage via sealed-secrets / external-secrets
  PG_PASSWORD: "CHANGEME"
